# Лабораторная работа №2  
**Обработка CSV и XML данных. Функции для анализа текстовой информации**

##  Описание
В этой работе реализован набор функций для анализа данных из CSV-файлов и извлечения структурированной информации из XML-документов.  
Вся логика вынесена в отдельные модули, что делает проект аккуратным и удобным для расширения.

##  Структура проекта

```
lab2/
├── main.py                # основной скрипт, демонстрирующий работу функций
├── text_analysis.py       # функции для работы с CSV и текстовыми данными
├── xml_utils.py           # функции для разбора XML
├── data/                  # входные данные
│     ├── books.csv
│     ├── books-en.csv
│     ├── books.xml
│     ├── civic.csv
│     └── currency.xml
└── results/               # выходные данные
      └── bibliographic.txt
```

---

## 🔧 Функциональность

### 1. Анализ CSV
Реализованы функции:

- **count_long_titles(path, min_len)**  
  Подсчёт книг с длинными названиями.

- **search_by_author(path, name)**  
  Поиск всех книг по заданному автору.

- **load_books(path)**  
  Загрузка списка книг с преобразованием полей.

- **generate_bibliography(books, n)**  
  Генерация выборки из *n* случайных библиографических записей.

- **save_bibliography(lines, path)**  
  Сохранение результата в текстовый файл.

- **top20_by_downloads(path)**  
  Формирование списка из 20 самых загружаемых книг.

- **unique_publishers(path)**  
  Получение списка уникальных издательств.

---

### 2. Работа с XML
Модуль `xml_utils.py` включает:

- **parse_currency(path)**  
  Разбор XML-документа с валютами  
  (извлечение кодов валют и их значений).

---

##  Запуск

Выполнить:

```
python main.py
```

Скрипт последовательно:

1. Показывает количество длинных названий  
2. Позволяет искать книги по автору  
3. Генерирует библиографический список  
4. Разбирает XML с валютами  
5. Выводит список издательств  
6. Показывает топ-20 книг по загрузкам

Результаты сохраняются в:

```
lab2/results/bibliographic.txt
```

---

##  Примечание
Проект имеет модульную структуру и легко расширяется:  
можно добавлять новые функции анализа и подключать дополнительные источники данных.
